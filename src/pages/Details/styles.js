import styled from 'styled-components';

export const CardDetails = styled.div`
  > p {
    line-height: 1.7;
  }

  .img-thumbnail {
    display: block;
    margin: 0 auto;
    max-width: 300px;
  }
`;
