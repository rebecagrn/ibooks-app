import styled from 'styled-components';

export const Bookcard = styled.div`
  justify-content: center;

  .img-thumbnail {
    margin: 10px;
  }

  .card-text {
    width: 280px;
    max-width: 400px;
  }
`;
